import React, { useEffect, useState } from "react";

type TSettingsUserCard = {
  cohort: string;
  email: string;
  name: string;
};

export function SettingsUserCard({ cohort, email, name }:TSettingsUserCard) {
  return <></>;
}
